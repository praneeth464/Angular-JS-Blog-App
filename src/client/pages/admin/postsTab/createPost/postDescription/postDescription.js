import postDescriptionComponent from './postDescription.component';

let postDescriptionModule = angular.module('postDescription', [])

.component('postDescription', postDescriptionComponent);

export default postDescriptionModule;
